import 'package:flutter/material.dart';

class SavedTrans extends StatelessWidget {
  const SavedTrans({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}